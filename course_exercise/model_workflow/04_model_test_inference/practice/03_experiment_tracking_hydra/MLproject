name: hydra_sweep

entry_points:
  main:
    parameters:
      hydra_options:
        description: hydra parameters to override
        type: str
        default: ''

    command: >-
      python main.py $(echo {hydra_options})